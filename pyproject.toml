[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]
# E501 (line too long) is enforced by the formatter, not the linter
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["custom_components.cover_extender"]
